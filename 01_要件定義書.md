# 家計簿自動化 要件定義書

| 項目 | 内容 |
|------|------|
| 文書名 | 家計簿自動化 要件定義書 |
| 版 | 0.2.3（草案） |
| 作成日 | 2026-07-24 |
| 更新日 | 2026-08-13 |
| 対象案 | 案B：Kotlin Backend API（Clean Architecture / DDD） |
| 参照データ | [家計簿（Google Sheets）](https://docs.google.com/spreadsheets/d/1ju8QljeAPxZIYCBAK8BjQdOlju4hQpX-HTsr5ZzVnJk/edit) |
| ステータス | レビュー待ち（未承認） |

---

## 1. 背景・課題

### 1.1 現状

現行の家計簿は、月ごとのシート（例: `25年7月`）に費目と金額を手入力し、合計・収支をシート内で管理している。  
月シートは 2024年2月〜将来月まで多数存在し、別途 `集計` / `時間集計` / `グラフ` タブもある。

現行（7月例）の主な費目:

- 固定寄り: 家賃、駐車場、車ローン、保険、携帯、光熱費 など
- 変動: 食費、外食、ガソリン、病院、生活費(カード) など
- 特別支出: 家具家電、Wi-Fi工事 など
- 収入: 収入列に記録（例: 150,000円）

### 1.2 課題

```text
【課題マップ】

① 運用負荷
   ・毎月シートを新規作成・コピペしている
   ・合計・収支の確認が手作業寄り

② 見通しの悪さ
   ・特別支出（家具・工事）が通常支出に混ざる
   ・月をまたいだ比較がしづらい

③ データ品質
   ・カテゴリ表記が揺れやすい（例: 駐車場が複数行）
   ・入力ルールがシートごとにぶれやすい
```

### 1.3 本プロジェクトで解決したいこと

1. **記録の場所を1つに統一**し、入力の手間を減らす
2. **合計・月次集計・収支を自動計算**する
3. **通常収支と特別支出込み収支を分けて見える化**する
4. **Kotlin / Clean Architecture / DDD の学習用土台**を作る
5. 将来の拡張（通知・スマホUI・Sheets連携など）のAPI土台を作る

### 1.4 方針転換（v0.1 → v0.2）

```text
v0.1 案A: Googleスプレッドシート内再設計
          ↓ 方針変更
v0.2 案B: Kotlin Backend API（Ktor）
         + Clean Architecture
         + DDD
```

- 業務ルール（固定費ひな形 / 特別FLG / 通常収支）は踏襲する
- 実装手段を Sheets 数式から **API + ドメインモデル** に切り替える
- 既存 Sheets は移行元データ・参照元として残す（当面の正本はAPI側DB）

---

## 2. 目的・ゴール

### 2.1 目的

Kotlin の Backend API 上で、家計の「入力」と「確認」を分離し、集計ルールをドメインに閉じ込めて自動化する。  
あわせて Clean Architecture / DDD の実践練習とする。

### 2.2 成功の定義（ゴール）

| # | ゴール | 達成イメージ |
|---|--------|--------------|
| G1 | 入力が簡単 | 取引登録 API で1件追加するだけ |
| G2 | 集計が自動 | 合計・収支・カテゴリ別をAPIが返す |
| G3 | 実態が見える | 特別支出を除いた「通常収支」が分かる |
| G4 | 月比較ができる | 複数月の収支を一覧で取得できる |
| G5 | 移行できる | 既存の7月データ相当を新形式で再現できる |
| G6 | 学習できる | 層分割とドメインモデルの意図が説明できる |

### 2.3 非ゴール（今回やらないこと）

- 銀行・クレジットカード明細の自動取得
- LINE Bot / Googleフォーム連携
- 家計簿アプリ（Money Forward等）との本格連携
- 本格的なフロントエンドアプリ（Web/Android）※別Phase
- Sheets への双方向同期
- 既存の全過去月シートの一括完全移行（必要に応じて段階移行）
- 高度な認証基盤（OAuth等）※学習初期は簡易で可

---

## 3. スコープ

### 3.1 対象範囲（In Scope）

```text
【In Scope】

[カテゴリ] [固定費] [取引入力] [月次集計] [サマリーAPI]
     │         │         │
     └──── 定義 ───→ 記録 ──→ 自動集計・返却
```

- Kotlin + Ktor による REST API
- Clean Architecture / DDD に沿ったモジュール構成（層名: rest / usecase / port / domain / driver）
- カテゴリマスタ・固定費マスタのCRUD
- 取引入力（1リクエスト=1取引）
- 特別支出フラグによる収支の二層表示
- 月次集計・サマリーAPI
- 現行7月データの移行検証（サンプル）
- ローカルで起動・動作確認できること

### 3.2 対象外（Out of Scope）

- 本番向けインフラ構築（当面ローカル/単一DB）
- スマホ専用アプリ / 本格Web UI
- 家計相談・節約アドバイス機能
- 複数ユーザー向けの本格権限設計

### 3.3 対象ユーザー

| 役割 | 想定人数 | 主な操作 |
|------|----------|----------|
| 家計管理者（本人） | 1 | API経由で入力・カテゴリ追加・確認 |
| 学習者（本人） | 1 | 設計・実装・リファクタの題材として利用 |

---

## 4. 技術方針

### 4.1 スタック

| 領域 | 採用 | 理由 |
|------|------|------|
| 言語 | Kotlin | 学習目的の主対象 |
| Web | **Ktor** | Kotlinファースト、Coroutine親和性が高い |
| アーキテクチャ | Clean Architecture | 依存の方向を内側（Domain）へ固定 |
| 設計 | DDD（軽量） | 家計ルールをドメインに閉じる |
| DB | MySQL（開発は Docker） | 指定スタック。Exposedからも利用しやすい |
| ORM/SQL | Exposed（予定） | Kotlin DSLで型安全に寄せやすい |
| ビルド | Maven | 慣れたツールで起動・依存管理しやすい |
| API形式 | REST + JSON | シンプルで検証しやすい |

### 4.1.1 開発条件（必須）

```text
1. Clean Architecture（依存は内側へ）
2. DDD 軽量（ルールは domain に閉じる）
3. イミュータブルモデリング（data class + val、変更は copy()）
4. TDD（Red → Green → Refactor。機能追加はテスト先行）
```

層名はヘキサゴナル寄りに揃える: `rest` / `usecase` / `port` / `domain` / `driver`  
（Cursor ルール: `.cursor/rules/kakeibo-architecture.mdc`）

### 4.2 Ktor を選ぶ理由（Springとの比較）

```text
学習目的で見るなら:

Ktor
  ・Kotlinらしい書き味（suspend / 拡張関数）
  ・構成が薄い → 層を自分で意識しやすい
  ・Clean Architecture練習に向く

Spring Boot
  ・情報量・採用例は多い
  ・自動設定が厚い → 「枠に乗せる」感覚になりやすい
  ・Java由来の慣習が混ざりやすい
```

本プロジェクトは **Kotlin学習 + 層設計の練習** が主目的のため **Ktor** を採用する。  
（将来、比較学習で Spring 版を作るのは任意）

### 4.3 全体構成（論理設計）

```text
┌──────────────────────────────────────────────────────────┐
│                     Ktor Application                      │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │     rest     │─▶│   usecase    │─▶│    domain    │   │
│  │  (Routing)   │  │ (UseCase)    │  │ (Entities /  │   │
│  └──────────────┘  └──────┬───────┘  │  Services)   │   │
│                           │          └──────▲───────┘   │
│                           ▼                 │           │
│                    ┌──────────────┐         │           │
│                    │     port     │─────────┘           │
│                    │ (Repository IF)                    │
│                    └──────▲───────┘                     │
│                           │                             │
│                    ┌──────────────┐                     │
│                    │    driver    │                     │
│                    │ (DB実装)     │                     │
│                    └──────────────┘                     │
└──────────────────────────────────────────────────────────┘
```

### 4.4 パッケージ構成（案）

```text
com.kakeibo
├── rest                  # Routing, DTO, HTTP
├── usecase               # UseCase（取引登録、月初反映、月次取得）
├── port                  # Repository interface
├── domain
│   ├── model             # Category, FixedCost, Transaction など
│   ├── service           # 集計ドメインサービス
│   └── exception
└── driver
    ├── db                # Exposed / migration
    └── repository        # Repository実装（アダプタ）
```

依存ルール: `rest → usecase → port ← driver`（いずれも `domain` を参照可。`domain` は外を知らない）

### 4.5 運用イメージ

```text
月初: 固定費マスタを取引へ反映（API呼び出し）
  ↓
普段: 変動費・特別支出・収入を取引登録
  ↓
確認: 月次サマリー / カテゴリ別APIを見る
```

---

## 5. 機能要件

### 5.1 機能一覧

| ID | 機能名 | 優先度 | 概要 |
|----|--------|--------|------|
| F-01 | カテゴリ管理 | Must | 費目をマスタ管理し、入力時に選択できる |
| F-02 | 固定費管理 | Must | 毎月発生する支出を一覧管理できる |
| F-03 | 取引入力 | Must | 日付・カテゴリ・金額等で取引を記録できる |
| F-04 | 特別支出区分 | Must | 一時的な大きな支出をフラグで区別できる |
| F-05 | 月次自動集計 | Must | 年月ごとの収入・支出・収支を自動計算する |
| F-06 | 通常収支表示 | Must | 特別支出を除いた収支を表示する |
| F-07 | カテゴリ別集計 | Should | 指定月のカテゴリ別支出を返せる |
| F-08 | サマリー取得 | Should | 今月のサマリーを1レスポンスで返せる |
| F-09 | 固定費の月次反映 | Should | 月初に固定費を取引へ転記できる |
| F-10 | データ検証 | Could | 必須項目欠落や不正値をエラー返却できる |
| F-11 | 過去データ移行 | Should | 既存月シートのデータを投入できる |
| F-12 | ヘルスチェック | Must | 起動確認用の簡易エンドポイント |

### 5.2 機能詳細

#### F-01 カテゴリ管理

- 費目をマスタとして登録・更新・一覧できる
- 取引登録時のカテゴリはマスタに存在するものに限定する
- 各大分類（住居 / 食 / 交通 / 特別 / 収入 など）を持てる

#### F-02 固定費管理

- 「毎月載せる項目」を管理する
- 有効/無効を切り替えられる（一時停止できる）
- 金額変更時は固定費マスタを直せば翌月以降の反映に使える

#### F-03 取引入力

- 1リクエスト1取引で記録する
- 必須: 日付 / カテゴリ / 金額 / 収支区分
- 任意: 特別FLG / 支払方法 / メモ

#### F-04 特別支出区分

- 家具家電・工事費など、単月のイレギュラー支出に `特別FLG = true` を付ける
- サマリーで「通常収支」と「特別込み収支」を併記する

#### F-05 / F-06 月次自動集計・通常収支

- 集計結果は永続化必須としない（都度計算でも可）
- 少なくとも以下を算出する
  - 支出合計
  - 収入合計（その他収入含む）
  - 通常収支（特別FLG=false の支出のみ差し引き）
  - 特別込み収支（全支出差し引き）

#### F-07 / F-08 可視化用API

- カテゴリ別支出一覧
- 月次推移一覧
- 今月サマリー（収入 / 通常支出 / 特別支出 / 収支）

#### F-09 固定費の月次反映

- Phase 1: `POST /fixed-costs/apply?yearMonth=YYYY-MM` のような明示APIで可
- 同じ年月への二重反映は防止する（要件）

#### F-11 過去データ移行

- まずは直近1ヶ月（例: 7月）を移行し、合計一致を確認する
- 過去月は必要に応じて段階移行する

---

## 6. データ要件

### 6.1 集約・エンティティ（DDD）

```text
【ドメインの中心】

Category（カテゴリ）
  └ 費目の辞書。取引が参照する

FixedCost（固定費）
  └ 毎月のひな形。有効なものだけ月初反映する

Transaction（取引）★中核
  └ 実際の収支1件。集計の材料

MonthlySummary（月次サマリー）
  └ 集計結果の読みモデル（エンティティでも値オブジェクトでも可）
```

### 6.2 モデル定義

#### 6.2.1 Category

| 項目 | 型 | 必須 | 説明 |
|------|----|------|------|
| id | UUID/Long | ○ | 識別子 |
| name | 文字 | ○ | 費目名（一意） |
| majorCategory | 文字 | ○ | 住居/食/交通/特別/収入 など |
| attribute | Enum | ○ | FIXED / VARIABLE / SPECIAL / INCOME |

#### 6.2.2 FixedCost

| 項目 | 型 | 必須 | 説明 |
|------|----|------|------|
| id | UUID/Long | ○ | 識別子 |
| categoryId | ID | ○ | カテゴリ参照 |
| amount | 金額 | ○ | 月額（円、整数） |
| enabled | Boolean | ○ | false は転記対象外 |
| memo | 文字 | - | 補足 |

#### 6.2.3 Transaction（中核）

| 項目 | 型 | 必須 | 説明 |
|------|----|------|------|
| id | UUID/Long | ○ | 識別子 |
| date | 日付 | ○ | 取引日。集計キー |
| categoryId | ID | ○ | カテゴリ参照 |
| amount | 金額 | ○ | 0より大きい整数 |
| balanceType | Enum | ○ | EXPENSE / INCOME / OTHER_INCOME |
| specialFlag | Boolean | ○ | 既定値 false |
| paymentMethod | 文字 | - | カード / 現金 / 口座振替 など |
| memo | 文字 | - | 自由記述 |

#### 6.2.4 MonthlySummary（算出項目）

| 項目 | 算出内容 |
|------|----------|
| yearMonth | `YYYY-MM` |
| expenseTotal | 当該月の支出合計 |
| incomeTotal | 収入 + その他収入 |
| normalBalance | 収入合計 − 特別以外の支出 |
| specialExpense | specialFlag=true の支出合計 |
| balanceIncludingSpecial | 収入合計 − 支出合計 |

### 6.3 集計ロジック（ドメインルール）

```text
対象月の支出合計
  = SUM( Transaction.amount | 日付が対象月 AND balanceType=EXPENSE )

対象月の収入合計
  = SUM( Transaction.amount | 日付が対象月 AND balanceType IN (INCOME, OTHER_INCOME) )

対象月の特別支出
  = SUM( Transaction.amount | 日付が対象月 AND balanceType=EXPENSE AND specialFlag=true )

通常収支
  = 収入合計 - (支出合計 - 特別支出)

特別込み収支
  = 収入合計 - 支出合計
```

※この計算は **Domain Service / Summary Factory** に置き、rest や driver に散らさない。

### 6.4 初期カテゴリ候補（現行7月ベース）

| カテゴリ | 大分類 | 属性 | 備考 |
|----------|--------|------|------|
| 駐車場 | 住居 | 固定 | 複数件あり得る |
| 車ローン | 交通 | 固定 | |
| 生活費(カード) | 生活 | 変動 | 将来明細化の余地あり |
| 電気代 | 光熱 | 固定 | 概算可 |
| ガス代 | 光熱 | 固定 | |
| 水道代 | 光熱 | 固定 | |
| ガソリン | 交通 | 変動 | |
| 食費 | 食 | 変動 | |
| 外食 | 食 | 変動 | |
| 保険 | 保険 | 固定 | |
| 病院 | 医療 | 変動 | |
| 床屋 | 美容 | 変動 | |
| 携帯 | 通信 | 固定 | |
| 小太郎ご飯 | ペット | 変動 | |
| 小太郎病院 | ペット | 変動 | |
| 家具家電 | 特別 | 特別 | 特別FLG対象 |
| 小樽の家に食費を入れる | 仕送り/支援 | 変動 | 名称は要相談 |
| 家賃 | 住居 | 固定 | |
| 火災保険 | 保険 | 固定/特別 | 年払いなら特別も検討 |
| Wi-Fi工事 | 特別 | 特別 | 特別FLG対象 |
| 給与 | 収入 | 収入 | 名称は実態に合わせて変更可 |

### 6.5 APIエンドポイント案（Phase 1）

| Method | Path | 概要 |
|--------|------|------|
| GET | `/health` | ヘルスチェック |
| GET/POST | `/categories` | カテゴリ一覧・登録 |
| GET/POST/PATCH | `/fixed-costs` | 固定費一覧・登録・更新 |
| POST | `/fixed-costs/apply` | 指定月へ固定費反映 |
| GET/POST | `/transactions` | 取引一覧・登録 |
| GET | `/summaries/{yearMonth}` | 月次サマリー |
| GET | `/summaries/{yearMonth}/by-category` | カテゴリ別集計 |

---

## 7. 非機能要件

| ID | 項目 | 要件 |
|----|------|------|
| N-01 | 利用環境 | ローカルで API を起動できること |
| N-02 | 操作性 | 取引1件の登録が簡単であること（curl/HTTPクライアントで確認可） |
| N-03 | 正確性 | 同一データなら現行合計と新集計が一致すること |
| N-04 | 可読性 | サマリーAPIだけで今月の状況が分かること |
| N-05 | 保守性 | カテゴリ追加がマスタ更新だけで完結すること |
| N-06 | 学習容易性 | 層の役割がディレクトリとクラス名から分かること |
| N-07 | コスト | 追加課金サービスなし（ローカルDocker中心） |
| N-08 | テスト | 集計ドメインロジックはユニットテスト可能であること |

---

## 8. 制約・前提

1. 現行スプレッドシートの閲覧権限があること（移行元）
2. 初期フェーズは **Ktor + MySQL + Exposed（予定）** で実装する
3. 固定費の自動バッチは必須としない（明示APIで開始可）
4. 金額は日本円、小数なしを基本とする
5. 1つの家計（個人/世帯）を対象とする
6. 認証は Phase 1 では簡易（またはなし）で開始してよい
7. DDDは過度な完全主義にせず、学習効果と実装速度のバランスを取る

---

## 9. 移行方針

```text
Phase 0  要件合意（本ドキュメント）
   ↓
Phase 1  API骨組み
         ・モジュール分割（rest/usecase/port/domain/driver）
         ・Category / Transaction / Summary
         ・7月データを投入し合計一致を確認
   ↓
Phase 2  固定費・見える化
         ・FixedCost / apply API
         ・特別FLG運用の定着
         ・カテゴリ別集計
   ↓
Phase 3  拡張（任意）
         ・過去月移行
         ・簡易UI（別リポジトリでも可）
         ・認証 / Sheetsエクスポート
```

### 移行時の受け入れ条件（Phase 1）

- [ ] 取引APIに7月相当データが入っている
- [ ] 支出合計が現行と一致する（例: 504,500）
- [ ] 収入合計が現行と一致する（例: 150,000）
- [ ] 特別込み収支が現行と一致する（例: -354,500）
- [ ] 特別支出を除いた通常収支が取得できる
- [ ] 集計ロジックのユニットテストがある

---

## 10. 受け入れ基準（全体）

以下を満たしたら「案Bの要件を満たした」とみなす。

1. 月シートを新しく作らなくても、年月指定で集計できる
2. 日常入力が取引登録APIだけで完結する
3. カテゴリ追加がマスタ更新で反映される
4. 通常収支と特別込み収支が同時に確認できる
5. 直近1ヶ月のデータで現行合計を再現できる
6. Domain に集計ルールがあり、driver に依存していない

---

## 11. リスクと対策

| リスク | 影響 | 対策 |
|--------|------|------|
| 学習範囲が広がりすぎる | 完成前に止まる | Phase 1は取引+集計に絞る |
| DDDのやりすぎ | 設計負債・迷子 | 集約は少数（Category/FixedCost/Transaction）に限定 |
| Sheetsとの二重管理 | どれが正か分からなくなる | 切替日を決め、以降はAPI側DBを正とする |
| カテゴリ名の揺れ | 集計漏れ | マスタID参照を強制 |
| 特別FLG付け忘れ | 通常収支が歪む | 特別カテゴリは初期値trueにする等の運用ルール |
| 過去データ移行の工数 | 着手遅れ | 直近1ヶ月のみ先行、過去は後回し |

---

## 12. 未決事項（要確認）

| # | 未決事項 | 選択肢・メモ | 決定期限 |
|---|----------|--------------|----------|
| Q1 | ID採番（UUID / Long） | 学習しやすさ優先ならLongでも可 | Phase 1前 |
| Q2 | 「生活費(カード)」はまとめのままか、明細化するか | 当面まとめ可 | Phase 1前 |
| Q3 | 火災保険は固定か特別か | 年払いなら特別候補 | Phase 2前 |
| Q4 | 収入の名称（給与/その他）をどう分けるか | 現行列に合わせる | Phase 1前 |
| Q5 | 過去何ヶ月分を移行するか | 当面は直近1ヶ月 | Phase 2後 |
| Q6 | 認証を入れるタイミング | Phase 1はなし/簡易 | Phase 2前 |
| Q7 | ORMは Exposed で確定か | Exposed / jOOQ など | Phase 1前 |
| Q8 | API検証ツール | curl / Bruno / Postman | 任意 |

---

## 13. 今後の成果物（この次）

| 順番 | 成果物 | 内容 |
|------|--------|------|
| 1 | 本要件定義書（v0.2） | 完了 |
| 2 | 設計書（ドメイン / API / DB） | 完了（[02_設計書.md](./02_設計書.md)） |
| 3 | プロジェクト骨組み | Maven + Ktor + 層ディレクトリ ★次ここ（手実装） |
| 4 | カテゴリ確定一覧 | 初期マスタの確定版 |
| 5 | ドメイン実装 + 集計テスト | 7月合計一致 |
| 6 | API実装 | CRUD + サマリー |
| 7 | 移行チェック結果 | 7月データの一致確認 |

---

## 14. 承認

| 役割 | 氏名 | 承認日 | サイン |
|------|------|--------|--------|
| 作成 | | 2026-07-26 | |
| 確認（本人） | | | |

### 変更履歴

| 版 | 日付 | 変更内容 | 著者 |
|----|------|----------|------|
| 0.1 | 2026-07-24 | 初版草案作成（案A: Sheetsベース） | - |
| 0.2 | 2026-07-26 | 案Bへ転換（Ktor + Clean Architecture + DDD）。固定費ひな形運用を明記 | - |
| 0.2.1 | 2026-07-26 | DBを PostgreSQL から MySQL に変更 | - |
| 0.2.2 | 2026-07-26 | ビルドツールを Gradle から Maven に変更 | - |
| 0.2.3 | 2026-08-13 | 開発条件（Immutable/TDD）と層名（rest/usecase/port/domain/driver）を明記 | - |

---

## 付録A. 現行7月データ（移行検証用）

| 費目 | 金額(支出) |
|------|------------:|
| 駐車場 | 9,900 |
| 車ローン | 33,000 |
| 生活費(カード) | 63,000 |
| 電気代 | 1,500 |
| ガス代 | 1,500 |
| 水道代 | 800 |
| ガソリン | 6,000 |
| 食費 | 20,000 |
| 外食 | 20,000 |
| 保険 | 10,000 |
| 病院 | 6,000 |
| 床屋 | 1,800 |
| 携帯 | 12,000 |
| 小太郎ご飯 | 8,000 |
| 小太郎病院 | 2,200 |
| 家具家電 | 170,000 |
| 小樽の家に食費を入れる | 20,000 |
| 家賃 | 65,000 |
| 駐車場 | 8,800 |
| 火災保険 | 20,000 |
| Wi-Fi工事 | 25,000 |
| **支出合計** | **504,500** |
| 収入 | 150,000 |
| **収支総合計** | **-354,500** |

---

## 付録B. 学習メモ（Kotlin / 設計）

```text
覚えたいポイント

1. 依存は内側だけを向く
   rest → usecase → port ← driver
                ↘ domain ↗
   driver は port（Domain 向け interface）を実装する

2. お金の計算は Domain に置く
   「通常収支」「特別込み」は UseCase や SQL に散らさない

3. 最初は小さく
   Transaction 登録と Summary 取得ができれば Phase 1 クリア
```
