# 家計簿自動化 設計書

| 項目 | 内容 |
|------|------|
| 文書名 | 家計簿自動化 設計書（ドメイン / API / DB） |
| 版 | 0.1.3（草案） |
| 作成日 | 2026-07-26 |
| 更新日 | 2026-08-13 |
| 対応要件 | [01_要件定義書.md](./01_要件定義書.md) v0.2.3 |
| 対象案 | 案B：Kotlin + Ktor + Clean Architecture + DDD |
| ステータス | レビュー待ち（未承認） |

---

## 1. 設計方針

### 1.1 ゴール

要件の業務ルールを、**Domain に閉じた形**で実装できる設計にする。  
学習しやすいよう、DDDは軽量（集約は少数）に抑える。

```text
【設計の約束】

1. 依存は内側だけ
   rest → usecase → port ← driver
                ↘ domain ↗

2. お金の計算は Domain
   通常収支 / 特別込み収支は Domain Service に置く

3. Phase 1 は小さく
   Category + Transaction + Summary を先に完成
   FixedCost apply は Phase 2
```

### 1.1.1 開発条件（必須）

| 条件 | 具体ルール |
|------|-----------|
| Clean Architecture | 依存は上図の方向のみ |
| DDD（軽量） | 家計ルールは `domain` に閉じる |
| イミュータブル | `data class` + `val`、変更は `copy()`、`var` 禁止 |
| TDD | Red → Green → Refactor。機能追加はテスト先行 |

層名: `rest` / `usecase` / `port` / `domain` / `driver`  
（Cursor ルール: `.cursor/rules/kakeibo-architecture.mdc`）

### 1.2 本設計で決めたこと（要件の未決を仮決定）

| # | 項目 | 決定 | 理由 |
|---|------|------|------|
| D1 | ID採番 | `Long`（DB採番） | 学習時の扱いが簡単 |
| D2 | ORM | Exposed | Kotlin DSLで層の境界が作りやすい |
| D3 | 認証 | Phase 1 なし | 学習範囲を絞る |
| D4 | 集計の保存 | 都度計算（テーブルなし） | 二重管理を避ける |
| D5 | 生活費(カード) | 当面まとめのまま | 現行データ踏襲 |
| D6 | 火災保険 | Phase 1 は固定扱い | 後で特別へ変更可 |
| D7 | APIプレフィックス | `/api/v1` | 将来変更に備える |
| D8 | DB | **MySQL 8** | 指定スタック（Dockerで起動） |

---

## 2. アーキテクチャ設計

### 2.1 論理構成

```text
┌─────────────────────────────────────────────────────────────┐
│                        Ktor Server                          │
│                                                             │
│  ┌──────────────────┐                                       │
│  │      rest        │  Routing / Request・Response DTO      │
│  └────────┬─────────┘                                       │
│           ▼                                                 │
│  ┌──────────────────┐                                       │
│  │     usecase      │  UseCase（手続きの司令塔）              │
│  └────────┬─────────┘                                       │
│           ▼                                                 │
│  ┌──────────────────┐     ┌──────────────────┐             │
│  │      port        │────▶│     domain       │             │
│  │  Repository IF   │     │  Model/Service   │             │
│  └────────▲─────────┘     └──────────────────┘             │
│           │                                                 │
│  ┌──────────────────┐                                       │
│  │     driver       │  Repository実装 / DB                   │
│  └──────────────────┘                                       │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 モジュール / パッケージ

単一 Maven モジュールで開始する（マルチモジュールは後回し）。

```text
com.kakeibo
├── KakeiboApplication.kt
├── rest
│   ├── route
│   │   ├── HealthRoutes.kt
│   │   ├── CategoryRoutes.kt
│   │   ├── FixedCostRoutes.kt
│   │   ├── TransactionRoutes.kt
│   │   └── SummaryRoutes.kt
│   ├── dto
│   └── error
│       └── ErrorResponse.kt
├── usecase
│   ├── category
│   │   ├── CreateCategoryUseCase.kt
│   │   └── ListCategoriesUseCase.kt
│   ├── fixedcost
│   │   ├── CreateFixedCostUseCase.kt
│   │   ├── UpdateFixedCostUseCase.kt
│   │   ├── ListFixedCostsUseCase.kt
│   │   └── ApplyFixedCostsUseCase.kt
│   ├── transaction
│   │   ├── CreateTransactionUseCase.kt
│   │   └── ListTransactionsUseCase.kt
│   └── summary
│       ├── GetMonthlySummaryUseCase.kt
│       └── GetCategorySummaryUseCase.kt
├── port
│   ├── CategoryRepository.kt
│   ├── FixedCostRepository.kt
│   └── TransactionRepository.kt
├── domain
│   ├── model
│   │   ├── Category.kt
│   │   ├── FixedCost.kt
│   │   ├── Transaction.kt
│   │   ├── Money.kt
│   │   ├── YearMonthVo.kt
│   │   ├── CategoryAttribute.kt
│   │   ├── BalanceType.kt
│   │   └── MonthlySummary.kt
│   ├── service
│   │   ├── MonthlySummaryCalculator.kt
│   │   └── FixedCostApplicator.kt
│   └── exception
│       └── DomainException.kt
└── driver
    ├── db
    │   ├── DatabaseFactory.kt
    │   ├── table
    │   │   ├── CategoriesTable.kt
    │   │   ├── FixedCostsTable.kt
    │   │   ├── TransactionsTable.kt
    │   │   └── FixedCostApplicationsTable.kt
    │   └── migration  （必要なら）
    └── repository
        ├── CategoryRepositoryImpl.kt
        ├── FixedCostRepositoryImpl.kt
        └── TransactionRepositoryImpl.kt
```

### 2.3 層ごとの責務

| 層 | やってよいこと | やってはいけないこと |
|----|----------------|----------------------|
| rest | HTTP入出力、DTO変換、ステータスコード | 集計計算、SQL直書き |
| usecase | ユースケース手順、port 呼び出し、トランザクション境界 | SQL直書き、HTTP詳細 |
| port | Repository などの interface | 実装・SQL |
| domain | エンティティ、値オブジェクト、集計ルール | Ktor / Exposed / JSON 依存 |
| driver | DBアクセス、port の実装 | 業務判断（特別FLGの意味など） |

---

## 3. ドメイン設計（DDD）

### 3.1 境界づけられたコンテキスト

```text
今回はコンテキストを分けすぎない。

┌─────────────────────────────────────┐
│     Household Budget Context        │
│     （家計簿コンテキスト）1つだけ      │
│                                     │
│  Category / FixedCost / Transaction │
│  MonthlySummary（読みモデル）         │
└─────────────────────────────────────┘

※ 認証・通知などは将来別コンテキスト候補
```

### 3.2 ユビキタス言語

| 用語 | 意味 |
|------|------|
| カテゴリ | 費目マスタ（家賃、食費など） |
| 固定費 | 毎月ほぼ同じ支出のひな形 |
| 取引 | 実際に発生した収支1件 |
| 特別支出 | 単月のイレギュラー支出（家具、工事など） |
| 通常収支 | 特別支出を除いた収支 |
| 特別込み収支 | 全支出を差し引いた収支 |
| 固定費反映 | 指定月に有効な固定費を取引へ転記すること |

### 3.3 集約と関連

```text
【集約】

Category（カテゴリ集約）
  id, name, majorCategory, attribute

FixedCost（固定費集約）
  id, categoryId ──参照──▶ Category
  amount, enabled, memo

Transaction（取引集約）★中核
  id, date, categoryId ──参照──▶ Category
  amount, balanceType, specialFlag
  paymentMethod?, memo?
  source（MANUAL / FIXED_COST_APPLY）
  fixedCostId?（反映元。手動なら null）

MonthlySummary
  ※永続化しない読みモデル。Transaction 群から計算
```

関連図：

```text
Category 1 ───────＊ FixedCost
    │
    │ 1
    │
    └──────────＊ Transaction ＊───── 0..1 FixedCost
                      │
                      ▼（集計）
                MonthlySummary
```

### 3.4 値オブジェクト / Enum

#### Money

```text
Money(amount: Long)
  - amount > 0
  - 単位は円（小数なし）
  - 加減算は同じ Money 同士のみ
```

#### YearMonthVo

```text
YearMonthVo(value: "YYYY-MM")
  - 例: 2025-07
  - Transaction.date から導出できる
```

#### CategoryAttribute

| 値 | 意味 |
|----|------|
| FIXED | 固定 |
| VARIABLE | 変動 |
| SPECIAL | 特別 |
| INCOME | 収入 |

#### BalanceType

| 値 | 意味 |
|----|------|
| EXPENSE | 支出 |
| INCOME | 収入 |
| OTHER_INCOME | その他収入 |

#### TransactionSource

| 値 | 意味 |
|----|------|
| MANUAL | 手入力 |
| FIXED_COST_APPLY | 固定費反映 |

### 3.5 エンティティ詳細

#### Category

| 項目 | 型 | 制約 |
|------|----|------|
| id | Long | 永続化後に採番 |
| name | String | 一意、空不可 |
| majorCategory | String | 空不可 |
| attribute | CategoryAttribute | 必須 |

ルール:
- 名前変更は可
- 削除は Phase 1 対象外（利用中参照があると危ないため、当面は更新のみ）

#### FixedCost

| 項目 | 型 | 制約 |
|------|----|------|
| id | Long | 採番 |
| categoryId | Long | Category 必須 |
| amount | Money | > 0 |
| enabled | Boolean | 既定 true |
| memo | String? | 任意 |

ルール:
- `enabled=false` は反映対象外
- 同一カテゴリの固定費を複数持てる（駐車場が2件など）

#### Transaction

| 項目 | 型 | 制約 |
|------|----|------|
| id | Long | 採番 |
| date | LocalDate | 必須 |
| categoryId | Long | Category 必須 |
| amount | Money | > 0 |
| balanceType | BalanceType | 必須 |
| specialFlag | Boolean | 既定 false |
| paymentMethod | String? | 任意 |
| memo | String? | 任意 |
| source | TransactionSource | 既定 MANUAL |
| fixedCostId | Long? | 反映時のみ |

ルール:
- 収入系（INCOME / OTHER_INCOME）の `specialFlag` は常に false
- カテゴリ属性が SPECIAL のとき、登録時の默认 `specialFlag=true` を推奨（UseCaseで補完）
- 固定費反映で作られた取引は `source=FIXED_COST_APPLY`

### 3.6 ドメインサービス

#### MonthlySummaryCalculator

入力: 対象月の `List<Transaction>`
出力: `MonthlySummary`

```text
expenseTotal
  = SUM(amount | EXPENSE)

incomeTotal
  = SUM(amount | INCOME or OTHER_INCOME)

specialExpense
  = SUM(amount | EXPENSE and specialFlag=true)

normalExpense
  = expenseTotal - specialExpense

normalBalance
  = incomeTotal - normalExpense

balanceIncludingSpecial
  = incomeTotal - expenseTotal
```

※ SQL で合計してもよいが、**計算式の意味はここに残す**
（driver は「月の取引を取る」まで、計算は Domain）

#### FixedCostApplicator

入力: 有効な `List<FixedCost>` + 対象 `YearMonthVo`
出力: 生成する `List<Transaction>`

```text
各 FixedCost(enabled=true) について
  Transaction(
    date = その月の1日,
    categoryId = fixedCost.categoryId,
    amount = fixedCost.amount,
    balanceType = EXPENSE,
    specialFlag = false,
    source = FIXED_COST_APPLY,
    fixedCostId = fixedCost.id,
    memo = fixedCost.memo
  )
```

### 3.7 ドメイン例外

| 例外 | 用途 | HTTP目安 |
|------|------|----------|
| NotFoundException | カテゴリ/取引が無い | 404 |
| ValidationException | 金額0以下、日付不正など | 400 |
| ConflictException | 固定費の二重反映など | 409 |
| DomainException | その他業務エラー基底 | 400/422 |

---

## 4. アプリケーション設計（ユースケース）

### 4.1 ユースケース一覧

| ID | UseCase | Phase | 概要 |
|----|---------|-------|------|
| UC-01 | CreateCategory | 1 | カテゴリ登録 |
| UC-02 | ListCategories | 1 | カテゴリ一覧 |
| UC-03 | CreateTransaction | 1 | 取引登録 |
| UC-04 | ListTransactions | 1 | 取引一覧（年月フィルタ） |
| UC-05 | GetMonthlySummary | 1 | 月次サマリー |
| UC-06 | GetCategorySummary | 2 | カテゴリ別集計 |
| UC-07 | CreateFixedCost | 2 | 固定費登録 |
| UC-08 | UpdateFixedCost | 2 | 固定費更新（金額・有効） |
| UC-09 | ListFixedCosts | 2 | 固定費一覧 |
| UC-10 | ApplyFixedCosts | 2 | 指定月へ固定費反映 |

### 4.2 主要フロー

#### 取引登録（UC-03）

```text
[Client]
   │ POST /transactions
   ▼
[TransactionRoutes]
   │ DTO → コマンド
   ▼
[CreateTransactionUseCase]
   │ 1. Category存在チェック
   │ 2. SPECIALカテゴリなら specialFlag を補完
   │ 3. Transaction 生成
   │ 4. Repository.save
   ▼
[Transaction]
```

#### 月次サマリー（UC-05）

```text
[Client]
   │ GET /summaries/2025-07
   ▼
[GetMonthlySummaryUseCase]
   │ 1. yearMonth の取引を取得
   │ 2. MonthlySummaryCalculator.calculate
   ▼
[MonthlySummary] → Response DTO
```

#### 固定費反映（UC-10）

```text
[Client]
   │ POST /fixed-costs/apply { "yearMonth": "2025-08" }
   ▼
[ApplyFixedCostsUseCase]
   │ 1. 既に反映済みなら Conflict
   │ 2. enabled=true の固定費を取得
   │ 3. FixedCostApplicator で Transaction 群を生成
   │ 4. 一括保存 + 反映履歴を記録
   ▼
[作成件数を返却]
```

二重反映防止:

```text
fixed_cost_applications
  year_month に UNIQUE

同じ年月で2回 apply → 409 Conflict
```

---

## 5. API設計

### 5.1 共通

| 項目 | 内容 |
|------|------|
| Base URL | `http://localhost:8080/api/v1` |
| 形式 | JSON |
| 日付 | `YYYY-MM-DD` |
| 年月 | `YYYY-MM` |
| 金額 | 整数（円） |
| 文字コード | UTF-8 |

#### エラーレスポンス

```json
{
  "code": "VALIDATION_ERROR",
  "message": "amount must be greater than 0",
  "details": []
}
```

| HTTP | code 例 |
|------|---------|
| 400 | VALIDATION_ERROR |
| 404 | NOT_FOUND |
| 409 | CONFLICT |
| 500 | INTERNAL_ERROR |

### 5.2 エンドポイント詳細

#### Health

| Method | Path | 説明 |
|--------|------|------|
| GET | `/health` | 起動確認（`/api/v1` 外でも可） |

Response:

```json
{ "status": "ok" }
```

---

#### Categories

| Method | Path | 説明 | Phase |
|--------|------|------|-------|
| GET | `/api/v1/categories` | 一覧 | 1 |
| POST | `/api/v1/categories` | 登録 | 1 |

POST Request:

```json
{
  "name": "食費",
  "majorCategory": "食",
  "attribute": "VARIABLE"
}
```

Response `201`:

```json
{
  "id": 1,
  "name": "食費",
  "majorCategory": "食",
  "attribute": "VARIABLE"
}
```

---

#### Transactions

| Method | Path | 説明 | Phase |
|--------|------|------|-------|
| GET | `/api/v1/transactions` | 一覧 | 1 |
| POST | `/api/v1/transactions` | 登録 | 1 |

Query（GET）:

| パラメータ | 必須 | 例 | 説明 |
|------------|------|----|------|
| yearMonth | - | 2025-07 | 指定月のみ |

POST Request:

```json
{
  "date": "2025-07-15",
  "categoryId": 8,
  "amount": 20000,
  "balanceType": "EXPENSE",
  "specialFlag": false,
  "paymentMethod": "現金",
  "memo": "スーパー"
}
```

Response `201`:

```json
{
  "id": 100,
  "date": "2025-07-15",
  "categoryId": 8,
  "amount": 20000,
  "balanceType": "EXPENSE",
  "specialFlag": false,
  "paymentMethod": "現金",
  "memo": "スーパー",
  "source": "MANUAL",
  "fixedCostId": null
}
```

---

#### Summaries

| Method | Path | 説明 | Phase |
|--------|------|------|-------|
| GET | `/api/v1/summaries/{yearMonth}` | 月次サマリー | 1 |
| GET | `/api/v1/summaries/{yearMonth}/by-category` | カテゴリ別 | 2 |

Response（月次）:

```json
{
  "yearMonth": "2025-07",
  "expenseTotal": 504500,
  "incomeTotal": 150000,
  "specialExpense": 195000,
  "normalExpense": 309500,
  "normalBalance": -159500,
  "balanceIncludingSpecial": -354500
}
```

※ 特別支出の内訳例（家具家電170,000 + Wi-Fi工事25,000 = 195,000）  
　火災保険を特別にするかは運用で変更可。

Response（カテゴリ別）:

```json
{
  "yearMonth": "2025-07",
  "items": [
    { "categoryId": 16, "categoryName": "家具家電", "amount": 170000 },
    { "categoryId": 18, "categoryName": "家賃", "amount": 65000 }
  ]
}
```

---

#### Fixed Costs

| Method | Path | 説明 | Phase |
|--------|------|------|-------|
| GET | `/api/v1/fixed-costs` | 一覧 | 2 |
| POST | `/api/v1/fixed-costs` | 登録 | 2 |
| PATCH | `/api/v1/fixed-costs/{id}` | 更新 | 2 |
| POST | `/api/v1/fixed-costs/apply` | 月次反映 | 2 |

POST Request（登録）:

```json
{
  "categoryId": 18,
  "amount": 65000,
  "enabled": true,
  "memo": "家賃"
}
```

POST Request（反映）:

```json
{
  "yearMonth": "2025-08"
}
```

Response `201`（反映）:

```json
{
  "yearMonth": "2025-08",
  "createdCount": 10
}
```

---

## 6. DB設計

### 6.1 ER図

```text
┌──────────────────┐       ┌──────────────────────┐
│ categories       │       │ fixed_costs          │
├──────────────────┤       ├──────────────────────┤
│ id PK            │◀──┐   │ id PK                │
│ name UK          │   │   │ category_id FK       │──┘
│ major_category   │   │   │ amount               │
│ attribute        │   │   │ enabled              │
│ created_at       │   │   │ memo                 │
│ updated_at       │   │   │ created_at           │
└──────────────────┘   │   │ updated_at           │
                       │   └──────────▲───────────┘
                       │              │
                       │              │ fixed_cost_id (nullable)
                       │              │
                       │   ┌──────────┴───────────┐
                       │   │ transactions         │
                       │   ├──────────────────────┤
                       └───│ category_id FK       │
                           │ id PK                │
                           │ date                 │
                           │ amount               │
                           │ balance_type         │
                           │ special_flag         │
                           │ payment_method       │
                           │ memo                 │
                           │ source               │
                           │ fixed_cost_id FK NULL│
                           │ created_at           │
                           │ updated_at           │
                           └──────────────────────┘

┌────────────────────────────┐
│ fixed_cost_applications    │
├────────────────────────────┤
│ id PK                      │
│ year_month UK              │
│ applied_at                 │
│ created_count              │
└────────────────────────────┘
```

### 6.2 テーブル定義

文字セット: `utf8mb4` / 照合順序: `utf8mb4_0900_ai_ci`（MySQL 8 既定で可）

#### categories

| 列 | 型 | 制約 | 説明 |
|----|----|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | |
| name | VARCHAR(100) | UNIQUE NOT NULL | 費目名 |
| major_category | VARCHAR(50) | NOT NULL | 大分類 |
| attribute | VARCHAR(20) | NOT NULL | FIXED/VARIABLE/SPECIAL/INCOME |
| created_at | DATETIME(3) | NOT NULL | |
| updated_at | DATETIME(3) | NOT NULL | |

#### fixed_costs

| 列 | 型 | 制約 | 説明 |
|----|----|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | |
| category_id | BIGINT | FK NOT NULL | → categories.id |
| amount | BIGINT | NOT NULL CHECK(amount > 0) | 円 |
| enabled | TINYINT(1) | NOT NULL DEFAULT 1 | boolean |
| memo | VARCHAR(255) | NULL | |
| created_at | DATETIME(3) | NOT NULL | |
| updated_at | DATETIME(3) | NOT NULL | |

#### transactions

| 列 | 型 | 制約 | 説明 |
|----|----|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | |
| date | DATE | NOT NULL | 取引日 |
| category_id | BIGINT | FK NOT NULL | → categories.id |
| amount | BIGINT | NOT NULL CHECK(amount > 0) | 円 |
| balance_type | VARCHAR(20) | NOT NULL | EXPENSE/INCOME/OTHER_INCOME |
| special_flag | TINYINT(1) | NOT NULL DEFAULT 0 | boolean |
| payment_method | VARCHAR(50) | NULL | |
| memo | VARCHAR(255) | NULL | |
| source | VARCHAR(30) | NOT NULL DEFAULT 'MANUAL' | |
| fixed_cost_id | BIGINT | FK NULL | → fixed_costs.id |
| created_at | DATETIME(3) | NOT NULL | |
| updated_at | DATETIME(3) | NOT NULL | |

Index:
- `idx_transactions_date` on `(date)`
- 年月フィルタは `date` の範囲検索（`>= 月初 AND < 翌月初`）で対応
- 二重反映ガード用:  
  **アプリケーション + `fixed_cost_applications.year_month` UNIQUE** で担保する

補足（駐車場2件）:
- 同じ `category_id` で取引が複数行あってよい
- 固定費も同じカテゴリで複数行あってよい

#### fixed_cost_applications

| 列 | 型 | 制約 | 説明 |
|----|----|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | |
| year_month | CHAR(7) | UNIQUE NOT NULL | `YYYY-MM` |
| applied_at | DATETIME(3) | NOT NULL | |
| created_count | INT | NOT NULL | 作成件数 |

### 6.3 マイグレーション方針

| Phase | 内容 |
|-------|------|
| 1 | categories / transactions を作成 |
| 2 | fixed_costs / fixed_cost_applications を追加 |

当面は Exposed の `SchemaUtils.create` でも可。  
慣れてきたら Flyway / Liquibase へ移行してよい。

---

## 7. 実装フェーズとクラス対応

```text
Phase 1（まずここ）
  domain  : Category, Transaction, Money, MonthlySummaryCalculator
  usecase : Create/List Category, Create/List Transaction, GetMonthlySummary
  driver  : categories, transactions
  rest    : /categories, /transactions, /summaries/{yearMonth}, /health
  Test    : 7月データで合計一致

Phase 2
  domain  : FixedCost, FixedCostApplicator
  usecase : FixedCost CRUD, ApplyFixedCosts, GetCategorySummary
  driver  : fixed_costs, fixed_cost_applications
  rest    : /fixed-costs*, /summaries/.../by-category
```

### 7.1 Phase 1 の TDD 順

```text
Step A  port（CategoryRepository / TransactionRepository）
Step B  usecase ★主戦場（InMemory な port 実装でテスト先行）
        CreateCategory → ListCategories
        CreateTransaction → ListTransactions
        GetMonthlySummary
Step C  driver（Exposed + MySQL で port を実装）
Step D  rest（/categories /transactions /summaries/{yearMonth}）
Step E  受け入れ（付録A合計一致）
```

各 Step は Red → Green → Refactor で進める。

---

## 8. テスト設計（要点）

### 8.1 必須（Phase 1）

| テスト | 内容 |
|--------|------|
| Unit | `MonthlySummaryCalculator` が付録Aの合計を再現 |
| Unit | Money が 0 以下を拒否 |
| API結合 | 取引登録 → サマリー取得 |

受け入れ数値（2025-07 想定）:

```text
expenseTotal             = 504500
incomeTotal              = 150000
balanceIncludingSpecial  = -354500
```

特別支出の扱い（推奨初期値）:

```text
家具家電 170000 → special=true
Wi-Fi工事 25000 → special=true
----------------------------
specialExpense = 195000
normalExpense  = 309500
normalBalance  = 150000 - 309500 = -159500
```

### 8.2 推奨（Phase 2）

| テスト | 内容 |
|--------|------|
| Unit | FixedCostApplicator が enabled のみ反映 |
| API結合 | apply 2回目が 409 |

---

## 9. ローカル実行構成

```text
┌────────────┐     ┌────────────┐
│ Ktor API   │────▶│ MySQL 8    │
│ :8080      │     │ :3306      │
└────────────┘     └────────────┘
      ▲
      │ curl / Bruno
   開発者
```

| 項目 | 値（案） |
|------|----------|
| DB名 | kakeibo |
| ユーザ | kakeibo |
| パスワード | kakeibo |
| JDBC | `jdbc:mysql://localhost:3306/kakeibo?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Tokyo` |
| ドライバ | MySQL Connector/J |

`docker-compose.yml` で MySQL 8 を立てる想定。

### 9.1 起動コマンド（Maven）

```text
# MySQL
docker compose up -d

# API
mvn -q compile exec:java

# 確認
curl http://localhost:8080/health
```

| 項目 | 値 |
|------|-----|
| ビルド | Maven |
| メインクラス | `com.kakeibo.KakeiboApplicationKt` |
| ポート | `8080` |

---

## 10. 学習ポイント（この設計で身につくこと）

```text
1. data class / value class でモデルを表す
2. interface を Domain に置き、実装を Infra に逃がす
3. UseCase が「手順」、Domain が「ルール」
4. suspend 関数で Repository を扱う（Ktor流）
5. テストしやすいように計算を純関数に寄せる
```

---

## 11. 未決・後回し

| # | 内容 | 扱い |
|---|------|------|
| R1 | 取引更新・削除API | Phase 2以降 |
| R2 | ページネーション | 件数増えてから |
| R3 | OpenAPI (Swagger) | 任意 |
| R4 | マルチモジュール化 | 学習後で可 |
| R5 | 認証 | Phase 3候補 |

---

## 12. 承認

| 役割 | 氏名 | 承認日 | サイン |
|------|------|--------|--------|
| 作成 | | 2026-07-26 | |
| 確認（本人） | | | |

### 変更履歴

| 版 | 日付 | 変更内容 |
|----|------|----------|
| 0.1 | 2026-07-26 | 初版（ドメイン / API / DB） |
| 0.1.1 | 2026-07-26 | DBを PostgreSQL から MySQL 8 に変更（型定義・接続情報を更新） |
| 0.1.2 | 2026-07-26 | ビルドツールを Gradle から Maven に変更。起動コマンドを追記 |
| 0.1.3 | 2026-08-13 | 開発条件（Immutable/TDD）と層名（rest/usecase/port/domain/driver）を明記。TDD順を追記 |
